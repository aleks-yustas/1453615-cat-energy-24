<li class="additional__item additional-item">
  <h3 class="additional-item__title">{{ additionalItem.title }}</h3>
  <p class="additional-item__count">{{ additionalItem.packaging }}</p>
  <p class="additional-item__price">{{ additionalItem.price }}&nbsp;Р.</p>
  <a class="additional-item__order button button--size_wide" href="#">Заказать</a>
</li>
